import { ID } from '~/models'
import { Nullable } from '~/utils'

import { LockedStatusVariant } from './types'

export const useCollectionLockedStatusVariant = (
  _collectionId: ID
): Nullable<LockedStatusVariant> => null
